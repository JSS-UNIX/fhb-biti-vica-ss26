name: "Destroy Infrastructure"

on:
  workflow_dispatch:

jobs:
  destroy:
    runs-on: ubuntu-latest

    steps:
      - name: Install Exoscale CLI
        run: |
          LATEST_URL=$(curl -s https://api.github.com/repos/exoscale/cli/releases/latest | grep "browser_download_url.*linux_amd64.tar.gz" | cut -d '"' -f 4)
          curl -sL "$LATEST_URL" | tar -xz
          sudo mv exo /usr/local/bin/exo

      - name: Delete VM and Security Group
        env:
          EXOSCALE_API_KEY:    ${{ secrets.EXOSCALE_API_KEY }}
          EXOSCALE_API_SECRET: ${{ secrets.EXOSCALE_API_SECRET }}
        run: |
          exo compute instance delete blakaj-vminfo --zone at-vie-1 --force
          exo compute security-group delete blakaj-vminfo-sg --force
